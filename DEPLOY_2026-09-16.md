# Deploy — 2026-09-16

## Files changed

- `index.html`
- `data/shopify_acquisition_summary.json`
- `.github/workflows/etl.yml`
- `README.md`
- `UPDATE_CECI_2026-09-16.md`

## Deploy steps

1. Replace/add the files above in the existing `smartrr-shopify-report` repository.
2. Commit and push to `main`.
3. Run **Actions -> Subscriptions ETL -> Run workflow** with `report_year = 2026`.
4. Wait for the action to finish green.
5. Refresh GitHub Pages.
6. Confirm tabs: **Smartrr YTD** and **Shopify Acquisition**.

## Secrets / variables

No new secrets are required. Keep the same Shopify + Smartrr credentials already used by the repository.

## Important

`shopify_acquisition_summary.json` is a management snapshot from the Jan-Aug 2026 Shopify attribution export. The current ETL validates and preserves the file but does not regenerate attribution data from the Shopify Admin report. Update this JSON when a new Performance by Referring Channel export is approved.
